<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Los campos scrollean; los botones quedan fijos abajo (pantallas chicas) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingTop="8dp"
            android:paddingEnd="24dp"
            android:paddingBottom="8dp">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="110dp"
                android:contentDescription="Logo Juguetería"
                android:src="@drawable/logo" />

            <TextView style="@style/Titulo" android:text="Configurar servidor" />

            <TextView
                style="@style/Etiqueta"
                android:layout_width="match_parent"
                android:text="Cargá la IP y el puerto de la PC donde corre la API."
                android:textColor="@color/gris"
                android:textSize="15sp" />

            <TextView style="@style/Etiqueta" android:text="IP:" />
            <EditText
                android:id="@+id/txtIp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="192.168.0.10"
                android:inputType="phone" />

            <TextView style="@style/Etiqueta" android:text="Puerto:" />
            <EditText
                android:id="@+id/txtPuerto"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="3000"
                android:inputType="number" />

            <TextView
                android:id="@+id/txtEstado"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:textColor="@color/gris"
                android:textSize="15sp" />
        </LinearLayout>
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="8dp"
        android:paddingEnd="24dp"
        android:paddingBottom="16dp">

        <Button
            android:id="@+id/btnProbar"
            style="@style/Boton.Lila"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Probar conexión" />

        <Button
            android:id="@+id/btnContinuar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="Continuar" />
    </LinearLayout>
</LinearLayout>
