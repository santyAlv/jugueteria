<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="32dp">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="220dp"
            android:layout_marginTop="24dp"
            android:contentDescription="Logo Juguetería"
            android:src="@drawable/logo" />

        <TextView style="@style/Titulo" android:text="Iniciar sesión" />

        <TextView style="@style/Etiqueta" android:text="Email:" />
        <EditText
            android:id="@+id/txtEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Mi Email..."
            android:inputType="textEmailAddress" />

        <TextView style="@style/Etiqueta" android:text="Contraseña:" />
        <EditText
            android:id="@+id/txtPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Mi Contraseña..."
            android:inputType="textPassword" />

        <Button
            android:id="@+id/btnIngresar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="Iniciar sesión" />

        <TextView
            style="@style/Etiqueta"
            android:layout_marginTop="36dp"
            android:text="Servidor (IP de la PC donde corre la API):"
            android:textColor="@color/gris"
            android:textSize="14sp" />
        <EditText
            android:id="@+id/txtServidor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="http://192.168.0.10:3000"
            android:inputType="textUri"
            android:textSize="15sp" />
    </LinearLayout>
</ScrollView>
